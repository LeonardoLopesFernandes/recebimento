<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
     android:maxSdkVersion="28" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
     android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_caminhao_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_caminhao_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recebimento"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Recebimento">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.LoginWebViewActivity"
            android:exported="false"
            android:theme="@style/Theme.Recebimento" />
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Recebimento" />
		<activity
            android:name=".ui.DetalhesViagemActivity"
			android:exported="false"
			android:theme="@style/Theme.Recebimento" />
		
		<activity
            android:name=".ui.ItensGuiaActivity"
			android:exported="false"
			android:theme="@style/Theme.Recebimento" />

        <activity
            android:name=".ui.ImagensRecebimentoActivity"
            android:exported="false"
            android:theme="@style/Theme.Recebimento" />

        <activity
            android:name=".ui.FotosRecebimentoActivity"
            android:exported="false"
            android:theme="@style/Theme.Recebimento" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="io.recebimento.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>